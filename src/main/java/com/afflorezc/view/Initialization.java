package com.afflorezc.view;

import javax.swing.*;
import java.awt.*;
//import javafx.embed.swing.JFXPanel;


import java.util.ArrayList;

import com.afflorezc.model.VehicleInformation;
import com.afflorezc.controller.ExcelLoader;

public class Initialization extends JFrame {
/*
    private static final String VIDEO_PATH = "files\\video\\SheldonDante.mp4";
    private final JFXPanel JFX_PANEL = new JFXPanel();

    private static ArrayList<VehicleInformation> motorcicleData;

    public Initialization() {

        setTitle("Sheldoon invoca a Dante!");
        setSize(800, 500);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);

        // Iniciar la carga de datos en segundo plano
        new ExcelLoader().execute();

        motorcicleData = ExcelLoader.getMotorcicleData();
        // Aquí deberías iniciar el video
        // Ejemplo: iniciarVideo();

        setVisible(true);
    }

    private void playVideo() {
        // Código para iniciar el video, depende de la librería que uses
    }

    public static ArrayList<VehicleInformation> getMotorcicleData() {
        return motorcicleData;
    }

    public static void main(String[] args) {
        new Initialization();
    }

 */
}
